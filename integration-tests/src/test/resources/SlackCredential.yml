services:
  slack-tnb:
    credentials:
      token: ${env.SLACK_WEBHOOK_TOKEN}
      channels:
        mc:
          webhook_url: ${env.SLACK_WEBHOOK_URL}
          channel_id: ${env.SLACK_CHANNEL}
    users:
      tnb: Slack